import React from 'react';

import Nav from './Nav';
import TopHead from './TopHead';

const Header = () => {
  
  return (    
      <header>
        <TopHead />
        <Nav />
      </header>   
  )    
};

export default Header;